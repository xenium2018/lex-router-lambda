{
  "name": "lex-router-lambda",
  "version": "1.0.0",
  "description": "AWS Lex Router Lambda for intent fulfillment routing",
  "main": "index.js",
  "dependencies": {
    "aws-sdk": "^2.1490.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "aws-sdk-mock": "^5.8.0"
  },
  "scripts": {
    "test": "jest"
  }
}